# This has not been implemented in your chosen language, so here's pseudocode
function euclid_sub(Int a, Int b){
    while (a != b){
        if (a > b){
            a = a - b
        }
        else
            b = b - a
        }
    }
}

# This has not been implemented in your chosen language, so here's pseudocode
function euclid_mod(Int a, Int b){
    Int temp
    while (b != 0){
        temp = b
        b = a%b
        a = temp
    }
}


